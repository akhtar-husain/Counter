# Counter
Add counter to your site using this jQuery plugin.

## Usage  
```JS
jQuery(function($) {
    $('.count').countTo({
        from: 2.50,
        to: 0.91,
        speed: 700,
        refreshInterval: 70,
		decimals: 2,
		separator: ','
    });
});
```
